<!--Our index page providing the content of our main page. Provides a display of two unordered lists of burgers based on 
whether or not they've been eaten. The list will access a partial that provides the name of the burger and a button for the user
to click on.-->

<div class="container-fluid mt-4">

    <div class="row text-center">

        <div class="col-md-6">
            <h2>BURGERS READY</h2>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                {{> burgers/burgers-eaten devoured=false}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>

        <div class="col-md-6">
            <h2>BURGERS EATEN</h2>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                {{> burgers/burgers-eaten devoured=true}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>

    <div class="row mt-5 text-center">
        <div class="offset-md-5 text-center form-group">
            <form class="make-da-burger">
                <label for="burger-input">Add a Burger:</label>
                <input type="text" name="new-burger" id="burger-input" class="form-control"
                    placeholder="Enter burger here">
                <input type="submit" value="Send the burger" class="btn btn-danger mt-3">
            </form>
        </div>
    </div>
</div>